apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  template:
    spec:
      containers:
        - name: auth-service
          image: auth-service:latest
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: auth-env
      imagePullSecrets: []   # <-- removes the secret
